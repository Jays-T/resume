document.ready = function() {
    toggleSections();
  }

function toggleSections() {
    let amountScrolled =
      document.getElementById("myBar").height();

    let 

    if amountScrolled === document.get

  }

  calculate links position on load, only re-calculate if screen size changes